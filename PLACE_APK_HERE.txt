Do not commit the APK to this folder.

Publish it as a GitHub Release asset named:

StreamDeck-IPTV-<version>.apk

Then update the JSON files under versions/. The main site, Fire TV page, and
Android app updater all resolve the newest release from that metadata.
