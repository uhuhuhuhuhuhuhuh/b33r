     } else {
         when {
-            shortestWidthDp >= 840 -> 1.16f
-            shortestWidthDp >= 600 -> 1.10f
-            shortestWidthDp < 340 -> 0.96f
-            else -> 1.02f
+            shortestWidthDp >= 840 -> 1.04f
+            shortestWidthDp >= 600 -> 1.00f
+            shortestWidthDp < 340 -> 0.92f
+            else -> 0.96f
         }
     }
     return ResponsiveScaleProfile(
@@ -167,18 +167,18 @@
             isTelevision = profile.isTelevision,
             uiScale = profile.uiScale,
             textScale = profile.textScale,
-            pagePadding = if (profile.isTelevision) 20.dp else 16.dp,
+            pagePadding = if (profile.isTelevision) 14.dp else 12.dp,
             gridCardWidth = when {
-                profile.isTelevision -> 178.dp
-                isTablet -> 172.dp
-                else -> 142.dp
+                profile.isTelevision -> 150.dp
+                isTablet -> 152.dp
+                else -> 126.dp
             },
-            gridSpacing = if (profile.isTelevision) 18.dp else 14.dp,
-            railWidth = if (profile.isTelevision) 108.dp else 104.dp,
-            loginMaxWidth = if (profile.isTelevision) 520.dp else 460.dp,
-            playerPadding = if (profile.isTelevision) 12.dp else 10.dp,
-            playerButtonSize = if (profile.isTelevision) 50.dp else 44.dp,
-            playerIconSize = if (profile.isTelevision) 34.dp else 30.dp,
+            gridSpacing = if (profile.isTelevision) 12.dp else 10.dp,
+            railWidth = if (profile.isTelevision) 88.dp else 84.dp,
+            loginMaxWidth = if (profile.isTelevision) 560.dp else 420.dp,
+            playerPadding = if (profile.isTelevision) 9.dp else 8.dp,
+            playerButtonSize = if (profile.isTelevision) 46.dp else 42.dp,
+            playerIconSize = if (profile.isTelevision) 30.dp else 28.dp,
         )
     }
 }
diff -urN '--exclude=*.png' '--exclude=*.webp' b33r-clean/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml b33r-src/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
--- b33r-clean/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml	2026-07-29 18:28:26.000000000 +0000
+++ b33r-src/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml	2026-07-29 19:21:29.407643913 +0000
@@ -1,5 +1,5 @@
 <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
     <background android:drawable="@color/launcher_background" />
-    <foreground android:drawable="@drawable/ic_launcher_foreground" />
-    <monochrome android:drawable="@drawable/ic_launcher_foreground" />
+    <foreground android:drawable="@drawable/ic_launcher_foreground_art" />
+    <monochrome android:drawable="@drawable/ic_launcher_foreground_art" />
 </adaptive-icon>
diff -urN '--exclude=*.png' '--exclude=*.webp' b33r-clean/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml b33r-src/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml
--- b33r-clean/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml	2026-07-29 18:28:26.000000000 +0000
+++ b33r-src/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml	2026-07-29 19:21:29.407643913 +0000
@@ -1,5 +1,5 @@
 <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
     <background android:drawable="@color/launcher_background" />
-    <foreground android:drawable="@drawable/ic_launcher_foreground" />
-    <monochrome android:drawable="@drawable/ic_launcher_foreground" />
+    <foreground android:drawable="@drawable/ic_launcher_foreground_art" />
+    <monochrome android:drawable="@drawable/ic_launcher_foreground_art" />
 </adaptive-icon>
